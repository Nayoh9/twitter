script(src="assets/js/profile.js")
div(class="profile-container me-3 bg-white d-flex flex-column")
    form( id="form-container" class="d-flex flex-row justify-content-center align-items-center" action="/users/update/image" method="post" enctype="multipart/form-data")
        input(type="file" name="avatar" class="d-none" id="input-avatar")
        img(src=currentUser.avatar class="img-fluid rounded-circle" id="image-profile")
    div(class="d-flex flex-row justify-content-center align-items-center my-2")
    p=`(${currentUser.username} ${currentUser.local.email})`  
    div(class="d-flex flex-row justify-content-center align-items-center my-2 text-center")
        div(class="flex-fill")
            p
            strong Tweet 
            br  
            span(class="text-primary")= tweets.length
        div(class="flex-fill")   
            p 
            strong Following 
            br
            span(class="text-primary") -